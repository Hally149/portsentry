# portsentry
A lightweight intrusion-detection tool that watches for port-scanning activity on a network and flags it as it happens — catching the kind of reconnaissance sweep that often shows up right before a more serious attack, so it doesn't go unnoticed.
